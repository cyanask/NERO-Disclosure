# 贡献指南

本仓库是信息披露 AI 系统的软件源码库。协作目标是让改动可复核、可回退，并保持业务资料留在作者或使用者的本机环境中。

## 分支与提交

1. 从最新 `main` 创建分支，命名建议 `feat/<主题>`、`fix/<主题>` 或 `codex/<主题>`。
2. 每次提交聚焦一件事，提交信息使用 `类型: 说明`，例如 `fix: 修正公司入口标识`。
3. 发起拉取请求，写明改动目的、影响范围、验证方式，以及尚未完成或需要人工确认的部分。
4. 由仓库维护者审阅合并。未经授权不要直接向 `main` 推送，也不要改写公共分支历史。

## 提交前检查

```sh
cd 01_app && ../.venv/bin/python -m pytest -q          # 默认测试，使用虚构样例
cd 01_app/frontend && npm run typecheck && npm run build
cd 01_app/runtime/pi && npm test                        # Pi 运行进程测试
```

`knowledge_pack` 标记的集成测试需要经授权的知识包，默认跳过；维护者按需在持有知识包的机器上运行。默认测试通过不等于知识库内容、真实模型效果或业务结论已验收。

## 不得提交的内容

- 知识库资料：法规原件、案例、监管决定、模板正式版、公司公告原件与提取正文、SQLite 检索投影。
- 业务与本机数据：事项库、会话库、文稿与交付物、执行日志、模型配置与密钥、证书与签名凭据。
- 由真实业务数据加工或抽样生成的“测试资料”；测试请使用 `01_app/tests/fixtures/` 下的虚构样例。
- 已安装的运行时依赖、前端构建产物（`frontend/dist/`）、缓存与压缩包。

需要新增测试资料时，请编写结构完整但内容虚构的样例，并在样例中标注用途与边界。

## 需维护者单独授权的事项

- 正式 App 的构建、安装、公证与对外发布。
- 修改知识包加载合同、数据库结构或生产路径。
- 引入新的第三方依赖（须先评估来源、版本、许可与维护状况）。
- 强制推送、历史改写、删除既有分支或标签。

## 代码约定

- 沿用既有目录、命名与检查方式，优先复用已有实现。
- 路径统一走 `backend/paths.py` 的三类根解析，不硬编码本机绝对路径。
- 涉及数据写入时保留原子写入、指纹校验和审计记录。
- 文档与说明写在 `01_app/docs/`，中文表述遵循仓库既有风格。
