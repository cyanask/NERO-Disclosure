# 本仓库的协作规则

本仓库是信息披露 AI 系统的**软件源码库**，供协作开发与代码审阅使用。规则用于约束在此目录工作的 Coding Agent（含 Codex 等），与使用者的项目规则同时生效。

## 资料边界

1. 本仓库只版本化 `01_app/` 软件与规则，以及根目录的导航、贡献与合作说明。
2. `02_knowledge/` 与 `03_local/` 是本机资料：前者是经授权的知识包，后者是事项、会话、文稿与交付物。两者都不进入提交，也不得被复制进仓库。
3. 默认测试与开发服务器使用 `01_app/tests/fixtures/knowledge/` 的虚构样例。样例内容为虚构，不得写入真实法规、公告、案例、公司名称或客户信息。
4. 不得提交模型密钥、证书、签名凭据、执行日志、数据库文件或任何由真实业务数据加工、抽样得到的资料。

## 开发与验证

- 路径解析统一使用 `01_app/backend/paths.py` 的三类根合同，不硬编码本机绝对路径。
- 默认测试：`cd 01_app && python -m pytest -q`；带 `knowledge_pack` 标记的集成测试需要经授权的知识包，默认跳过。
- 前端：`cd 01_app/frontend && npm run typecheck && npm run build`；`frontend/dist/` 是构建产物，不提交。
- Pi 运行进程：`cd 01_app/runtime/pi && npm test`。
- 改动保持最小范围并沿用既有风格；数据写入保留原子写入、指纹校验与审计记录。

## 需要使用者单独授权的事项

- 正式 App 的构建、安装、公证、对外发布，以及任何向外部系统推送或发送的动作。
- 修改知识包加载合同、数据库结构或生产路径。
- 引入新的第三方依赖、修改全局配置或安装常驻服务。
- 删除既有资料、强制推送或改写 Git 历史。

## 汇报

说明改了什么、如何验证、还有哪些未验证或需要人工确认的部分；默认测试通过只代表确定性检查通过，不代表知识库内容、真实模型效果或业务结论已经验收。
