# 页面重启与版本校验

“系统治理 → 版本与变更”最右侧的深色按钮重启当前本机服务。`scripts/run.py` 负责在 Uvicorn 完成请求排空和运行时关闭之后，以原 Python 解释器、原端口、原数据目录和原启动参数重新执行。页面不接收命令、路径或 PID，也不会重启其他服务。

模型任务（含轮次收尾）、模型登录、完整校验或其他写请求尚未完成时，服务拒绝重启并给出原因。重启一经接纳，新的业务请求返回 503；状态探测及重新建立本机会话仍可访问。现有请求由服务正常排空，不强制终止事务。

请求必须通过现有本机 Origin、网页会话和 CSRF 校验，并携带当前服务实例编号。同一实例的重复请求只触发一次；旧实例请求不会重启新服务。接口为 `GET /api/governance/service/restart` 和 `POST /api/governance/service/restart`，POST 请求体仅为 `{"instance_id":"当前编号"}`。

页面观察到新的实例编号后，重新获取 CSRF 会话并刷新版本信息，保留当前页签。POST 响应丢失时只探测状态，不自动重发。60 秒未恢复时提供“重试连接”；启动失败时需检查启动终端或重新运行项目启动入口。直接以其他 Uvicorn 命令启动的服务不会提供可点击的重启能力；从旧版本首次升级仍需通过启动入口加载一次新代码。

完整校验继续在后台执行。进入版本页只读取现有校验状态，不自动发起校验；运行中自动更新进度，连接失败才显示重试入口。“停止校验”保留为显式操作。

验收：隔离端口实测重启、会话保留、新实例和新 CSRF；本机页面实测点击重启、自动重连、版本刷新、完整校验进度及切换页签后恢复。保护逻辑由 `tests/test_service_restart.py` 覆盖。未将本机 macOS 验收等同于 Windows 运行验收。
