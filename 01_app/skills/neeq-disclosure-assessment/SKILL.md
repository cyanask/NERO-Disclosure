---
name: neeq-disclosure-assessment
description: 为本系统基础层、创新层和创业板事项开展第一节点披露义务分析，使用冻结事实、适用法源与规则检查提出待专业复核的判断；不处理清单编制或公告定稿。
---

> 历史方法：外部宿主接入已移除；当前方法以 skills/registry.json 登记为准。

# 披露义务判断

本Skill只属于当前信息披露MVP，由assessment任务绑定并交给外部宿主执行。输入、领取凭证和输出schema以get_agent_task_context返回的本次任务包为准。

先把事项拆成主体、层级、事项日期、触发事实、金额及口径、相关主体与已有披露。区分已经证实、提供者声明、存在矛盾和缺失的事实。不能从一个空白字段推定否定事实，也不能把模拟公司或案例事实替换为本公司事实。

检索laws并按来源ID读完整条文。核对发布、生效及适用时点，连同定义、例外、过渡安排和交叉义务分析。当前资料包只有登记版本，历史或专项覆盖不足必须说明；没有命中不等于没有义务。案例可作解释线索，不能作为本节点的规范依据。

结合rule_check核对金额、分母、累计期间、去重、比较符号和最先触发时点。它只覆盖已经编码的切片，不是AI结论；阈值未命中仍应核对重大性、特殊事件、子公司及进展披露。交易日需可靠日历，不以自然日或工作日替代。对主张豁免逐一检查事实是否满足，不把“未达到本项阈值”写成“无需披露”。

有实质影响的缺失或矛盾事实，返回needs_info并在missing列出需补字段。事实充分时提出disclose或no_disclosure；法源冲突、覆盖不足、重要例外不明或专业判断不能收敛时返回review_required，并说明需要裁决的问题。不得通过选择某个状态绕过后端检查。

提交summary、reasons、missing、source_ids、limitations等任务schema字段。理由应把事实与具体条款联系起来，并交代触发、例外及后续披露；无需披露结论尤其要说明覆盖范围和未来何种变化需重评。引用仅限任务绑定法源包的有效ID；新增资料先按项目维护流程接纳，再领取新版本任务。


## Verify/Gate 与缺失法源

先读取 verification_gate 或调用 verify.run。法规库（R1）提供权威法源；案例库、文种 profile 与版式规则（R2）约束生产。法规时效不符直接停止推进，回法规库读完整条文、核对适用期间与核验截止日期。库中缺失时由当前外部 Agent 使用自己的互联网检索工具查官方原文；不得把候选片段或案例当法源。

检索到适用版本后，先用 library.manage/update 收录法规库（含官方 URL、正文、生效/失效日期、核验日期；替代版本须新 id 并登记 replaces_source_ids），必要时 law.bind 绑定本事项版本，再重建任务。检索不到或宿主无法联网时，调用 source.search_report，填写本节点 stage、verify.run 的 input_fingerprint、实际查询、原文地址和原因，反馈用户并停止后续节点。found 只记录线索，不会放开 Gate；not_found 不得继续生产。

通过 task.submit 留存候选，task.adopt 在当前版本 Verify 通过后才载入；gate.advance 再核对事实、法源及上游依赖。Web 与 Agent 使用同一 Gate，均无人工权限或强制放行入口。PASS 仅代表已执行的确定性检查通过，不等于法律意见、人工验收或已发布。

## 板块与知识库边界

工作流与工具在三板块共用，法源、案例、模板按任务包的 board 独立绑定。所有 library.search/read/manage/update、templates.list 必须传入该 board；同名条目在不同板块仍是独立记录，不使用 market=neeq 或 common 合并池。更新只作用于指定板块；不得用其他板块的资料或旧任务快照补齐缺口。

初始拆分保留重叠资料；收录范围不等于逐条适用性已核对。基础层、创新层案例的发布时层级尚待核实。创业板 imported_source_unverified 案例缺少可核验官方原件，只能作为待核验线索，不得用于已核实实践断言或法律依据；模板与导入版式保持待复核状态，不能把历史标签或机械通过当成专业验收。
